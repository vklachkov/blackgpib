[package]
name = "blackgpib"
version = "1.0.0"
edition = "2024"

[dependencies]
log = { version = "0.4.28", features = ["max_level_debug", "release_max_level_debug", "std"] }
rppal = "0.22.1"
thiserror = "2.0.17"

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
